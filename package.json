{
	"name": "aliexpress-scraper",
	"version": "1.0.0",
	"description": "",
	"main": "dist/index.js",
	"type": "module",
	"scripts": {
		"build": "rm -rf dist && tsc",
		"build:watch": "rm -rf dist && tsc -w",
		"dev": "npm run build:watch & nodemon . --ignore dumps",
		"start": "npm run build && node ."
	},
	"keywords": [],
	"author": "LacheRo`",
	"license": "MIT",
	"devDependencies": {
		"@types/node": "^18.11.18",
		"nodemon": "^2.0.20",
		"typescript": "^4.9.5"
	},
	"dependencies": {
		"axios": "^1.3.0"
	}
}
